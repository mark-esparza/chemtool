# P0 science service dependencies.
# Pinned to compatible major ranges; regenerate a lockfile (pip-tools) before deploy.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
rdkit>=2023.9
